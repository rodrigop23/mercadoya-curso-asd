## Commits

Cuando se solicite un commit, usa `type(scope): resumen`. `type` y `scope` van en inglés; el resumen y el cuerpo, en español. Incluye solo archivos del cambio y mantén al humano como único autor, sin trailers de herramientas de IA.
