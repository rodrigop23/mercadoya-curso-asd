# MercadoYa API

Scaffold Hono para Node.js creado con `create-hono` y el template `nodejs`.

Desde la raíz del monorepo, ejecuta `pnpm --filter @mercadoya/api dev`. La API responde en <http://localhost:3001>.
